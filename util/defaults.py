import pyglet

# Default AudioFormat
defaultAudioFormat = pyglet.media.codecs.AudioFormat(channels=2,
                                                     sample_size=16,
                                                     sample_rate=44100)
